import NewsGeneratorClient from "@/components/news/NewsGeneratorClient";

export const metadata = { title: "Notícias — Mídia Vertical" };

export default function NoticiasPage() {
  return <NewsGeneratorClient />;
}
