import WeatherGeneratorClient from "@/components/weather/WeatherGeneratorClient";

export const metadata = { title: "Clima — Mídia Vertical" };

export default function ClimaPage() {
  return <WeatherGeneratorClient />;
}
